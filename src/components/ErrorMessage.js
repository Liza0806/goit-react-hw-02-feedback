
export const Error= () => {
  
  const titleStyle = {
    color: '#333',
    fontSize: '20px',
    marginBottom: '10px',
  };
      return (<span style={titleStyle}>There is no feedback</span>);    
    
    };